#!/usr/bin/env python3
"""
Ludwig CLI Entry Point
"""
import sys
import os

# Add src to path so we can import Ludwig modules
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))

from cli.artisan import main

if __name__ == "__main__":
    main()
