# UsersController - RESTful API Controller
# Generated on 2025-06-18 22:02:32

let UsersController = create api_controller do
    let index = create action do
        # GET /api/users - List all resources
        return json_success("List of users")
    
    let store = create action do
        # POST /api/users - Create new resource  
        return json_success("Created user")
    
    let show = create action do
        # GET /api/users/{id} - Get specific resource
        return json_success("Show user")
    
    let update = create action do
        # PUT /api/users/{id} - Update resource
        return json_success("Updated user")
    
    let destroy = create action do
        # DELETE /api/users/{id} - Delete resource
        return json_success("Deleted user")
